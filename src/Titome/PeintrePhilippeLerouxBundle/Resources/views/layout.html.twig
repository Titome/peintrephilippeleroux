{% extends '::base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    
    {% stylesheets 
        '@TitomePeintrePhilippeLerouxBundle/Resources/public/css/peintrephilippeleroux.css'
        '@TitomePeintrePhilippeLerouxBundle/Resources/public/css/peintrephilippeleroux-responsive.css'
        '@TitomePeintrePhilippeLerouxBundle/Resources/public/css/bootstrap-responsive.min.css'
        '@TitomePeintrePhilippeLerouxBundle/Resources/public/css/bootstrap.min.css'
        'bundles/titomepeintrephilippeleroux/zoombox/zoombox.css'
         filter='cssrewrite' output='style.css'
    %}
        <link href="{{ asset_url }}" rel="stylesheet" />
    {% endstylesheets %}
{% endblock %}

{% block body_class %}
    {{ parent() }}
        
    class="blackB"
{% endblock %}
    
{% block nav %}
    <div class="navbar-red">
        <div class="navbar-inner">
            <div class="container">
                {# TODO: Finir la page d'accueil du site #}
                <a class="brand underline" href="{{ path('Accueil') }}">PHILIPPE LEROUX</a>
                <ul class="nav">
                    {# EXPO
                    <li class="dropdown fontSize20px"><a href="{{ path('Expo') }}">Exposition</a></li>#}
                    <li class="dropdown fontSize20px"><a href="{{ path('Actu') }}">Actualité</a></li>
                    <li class="dropdown fontSize20px"><a href="{{ path('Galerie') }}">Galerie</a></li>
                    <li class="dropdown fontSize20px"><a href="{{ path('Livre') }}">Livres</a></li>
                    <li class="dropdown fontSize20px"><a href="{{ path('Contact') }}">Contact</a></li>
                    <li class="dropdown fontSize20px"><a href="{{ path('Bio') }}">Biographie</a></li>                    
                </ul>
                <ul class="nav pull-right">
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        <li><span>Bonjour {{ app.user.username }} !</span></li>
                        <li><a href="{{ path('AccueilAdmin') }}">Adminstration</a></li>
                        <li><a href="{{ path('fos_user_security_logout') }}">Déconnexion</a></li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </div>
{% endblock %}